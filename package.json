{
  "name": "erp-management-backend",
  "version": "1.0.0",
  "main": "server/index.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install",
    "build-client": "cd client && npm run build",
    "start": "node server/index.js",
    "dev": "concurrently \"nodemon server/index.js\" \"cd client && npm start\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "pdfkit": "^0.13.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  }
}
